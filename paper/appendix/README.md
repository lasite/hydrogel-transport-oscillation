# Migrated Appendix

Status: migrated source material, not validated.

`appendix.tex` preserves the appendix fragment extracted from
`../paper_latest/paper_latest/main.tex` lines 1385-2192. The extraction starts
at `\appendix` and stops before the source file's final `\end{document}` so the
fragment can later be included in a manuscript wrapper.

No equations or physical claims have been edited during migration.
