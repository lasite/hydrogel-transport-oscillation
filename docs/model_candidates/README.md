# Model Candidates

This directory stores candidate model descriptions during the exploratory stage.

Multiple candidates may coexist here. A model should remain in this directory until it has passed sanity checks, limiting-case checks, and the relevant verification gates.

Do not move a candidate model into `docs/validated/` unless a task explicitly requires promotion and records the evidence.
